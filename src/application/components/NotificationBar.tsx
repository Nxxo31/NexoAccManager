// Application Component: NotificationBar — toast system
// Replaced by Mantine Notifications, so this component returns null.

import { useUIStore } from '../store/uiStore';

export function NotificationBar(): JSX.Element | null {
  return null;
}